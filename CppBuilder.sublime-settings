{
    //header files
    "include_dir": [
    ],

    //library directories 
    "lib_dir": [
    ],

    //library names
    "lib_names": [
    ],

    //the name of the file to generate (the final exe)
    "main_file": "App.exe",

    //additional flags for the compiler
    "additional_flags": [
        "Wall"
    ]
}
